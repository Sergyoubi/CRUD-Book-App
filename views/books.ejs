<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> <%=title%> </title>
  <link rel="stylesheet" href="">
</head>
<body>

  <div class="container">

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">  <a class="nav-link" href="/about">About the App</a>  </li>       
        <li class="nav-item">  <a class="nav-link" href="/books">See all Books</a>  </li> 
      </ul>
    </nav>

  </div>

  <h2>List of Books (<%= model.length %>)</h2>

  <table>
    
    <thead>
      <tr>
        <th> Author </th>
        <th> Title </th>
        <th> Comment </th>
        <th> <a href="/create/">Add</a> </th>
      </tr>
    </thead>
    <tbody>

      <% for ( const book of model) { %>
        
        <tr>
          <td> <%= book.author %> </td>
          <td> <%= book.title %> </td>
          <td> <%= book.comment %> </td>
          <td>
            <a href="/edit/<%= book.book_id %>">Edit</a>
            <a href="/delete/<%= book.book_id %>">Delete</a>
          </td>
        </tr>

      <% } %>
    </tbody>

  </table>

</body>
</html>